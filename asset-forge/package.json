{
  "name": "zta-asset-forge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/admin.js && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply DB --remote --yes",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.79.0"
  }
}
