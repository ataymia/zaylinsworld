# dependencies
node_modules/

# build output
dist/

# editor / os
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json

# local env
.env
.env.local

# raw asset source archives + high-res originals (extracted/processed into
# public/assets/; keep the lean web copies in git, not the multi-100MB sources)
/*.zip
vendor/source-textures/
