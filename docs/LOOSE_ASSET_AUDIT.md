# Loose Asset Audit

_Generated by `tools/audit-loose-assets.mjs` (`npm run audit:loose`). Read-only audit of loose 3D-asset sources in the repo root and `./.staging`. Re-run after dropping new packs._

## Summary

- Model sources found: **0**
- Texture siblings found: **0**
- Routable (rule matched): **0**
- Needs review (no rule): **0**

## Model sources

_None found. Drop packs into `./.staging` or the repo root, then re-run `npm run audit:loose`._

## Notes

- This audit never moves or deletes files; run `npm run organize` to route staged packs.
- Root-level raw sources (`*.zip/*.rar/*.obj/*.blend/*.fbx`) are gitignored, so commit lean converted GLBs under `public/assets/models/**` rather than the heavy originals.
- Do **not** delete `.bin`, textures, or credit/license files.
